# Versions used for the results reported in the paper.
# Python 3.12

# --- analysis and modelling
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.7.0
rdkit==2025.9.5
torch==2.5.1

# --- figures
matplotlib==3.10.0
seaborn==0.13.2

# --- only needed to regenerate the C2S cell sentences (requires a GPU;
#     the released archive already contains them, so this is optional)
transformers==4.57.1
accelerate
bitsandbytes
